.comment
  %a.comment-left{ href: "#{comment.user}"}
    = image_tag comment.user.profile.image_url, class: "media-object img-rounded", alt: "Generic placeholder image"
  .comment-body
    %h4.comment-heading= link_to comment.user.name, comment.user
    %p= comment.body
    %p.text-muted 
      posted 
      = time_ago_in_words comment.created_at
      = link_to "like", ""
      = link_to "reply", comment