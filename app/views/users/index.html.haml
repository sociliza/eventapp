%h1 Party Rockers
.row
  - @users.each do |user|
    - if user.id != current_user.id
      .item.col-sm-4
        - if user.profile.image.present?
          = image_tag(user.profile.image_url)
        - else
          =  image_tag "http://placehold.it/350x250", alt: ""
        %p= link_to user.name, user
        %p= link_to "Start a conversation with me!", conversations_path(sender_id: current_user.id, recipient_id: user.id), method: :post
     
