%section
  %section.col-lg-12.col-md-12
    %h1 Upcoming Events
    %p= link_to "Plan Business", home_index_path

    - if business_signed_in?
      = link_to 'Create A New Event', new_business_event_path(current_business)

  %section.col-lg-12.col-md-12
    = render @events